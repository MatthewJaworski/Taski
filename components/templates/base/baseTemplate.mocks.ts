import { BaseTemplateProps } from './BaseTemplate';

const base: BaseTemplateProps = {
  sampleTextProp: 'Sample text',
};

const alternateBase: BaseTemplateProps = {
  sampleTextProp: 'Alternate sample text',
};

export const mockBaseTemplateProps = {
  base,
  alternateBase,
};
