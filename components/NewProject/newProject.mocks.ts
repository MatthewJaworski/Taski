import { NewProjectProps } from './NewProject';

const base: NewProjectProps = {};

const alternateBase: NewProjectProps = {};

export const mockNewProjectProps = {
  base,
  alternateBase,
};
